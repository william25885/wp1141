(1) csv裡面存的是1w筆資料，想要插入建議先把舊的DB給刪掉
    先直接在psql的terminal下
    DROP DATABASE bello;
    再重新跑一次init_bello_db.sql

(2) 這邊直接採取前後端分離的方式，前後端都是獨立的
    
(3) 前端是vue3，所以需要先載node.js

(4) 後端是flask，你那邊環境應該有

(5) 在測試的時候 如果想要同時開兩個user模擬聊天情境的話，不能在同一個瀏覽器開兩個tab登入不同帳號 會有問題 這時間不夠處理不了(切記)
    建議開兩個瀏覽器 (safari and chrome) 又或是多開幾個無痕視窗登入就好 登入統一在 127.0.0.1:5000的頁面

(6) 理想Demo情況會是有三個視窗 2個user 1個admin，可以先自己模擬一下